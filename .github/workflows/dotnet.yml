name: dotnet package

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: welcome
        with:
          dotnet-version: '8.x'
      - run: echo "welcome"
