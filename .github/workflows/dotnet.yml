name: dotnet package

on: [push]

jobs:
  welcome:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "welcome"
    
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: welcome
        with:
          dotnet-version: '8.x'
      - run: echo "welcome"
